#include "MainController.h"

namespace scn::app::controller
{
// The controller is intentionally thin: lifecycle and source transactions
// remain owned by MonitoringSession rather than by Qt widgets.
}
