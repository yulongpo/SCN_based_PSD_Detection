# Runtime configuration

`config/legacy/HaiAISpecMonitor` preserves the original ISA JSON resources for
reference during UI migration. The active Qt 6 skeleton does not load the old
Flow configuration; source and application configuration currently come from
the UI controls and `SourceConfig`.
