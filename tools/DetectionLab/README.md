# DetectionLab

This standalone tool uses the same `ISpectrumSource` and `DetectionEngine`
interfaces as the product. It is intentionally small until the algorithm
implementation is added; it is the future deterministic replay/debug entry.
